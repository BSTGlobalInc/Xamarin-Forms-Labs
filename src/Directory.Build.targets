<Project>
  <Target Name="PublishPackages" AfterTargets="AfterBuild" Condition="'$(ProjectExt)' == '.nuproj'">
    <Copy
        SourceFiles="$(TargetPath)"
        SkipUnchangedFiles="true"
        DestinationFolder="$(LocalNuGetRepository)"
        />
  </Target>
</Project>